<UserControl x:Class="WPF_Student_Management.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:WPF_Student_Management.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:WPF_Student_Management.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F5F6FA">

    <UserControl.DataContext>
        <viewmodels:SettingsViewModel />
    </UserControl.DataContext>

    <Grid Margin="30">
        <materialDesign:Card UniformCornerRadius="12" Padding="40,30" MaxWidth="450" HorizontalAlignment="Center" VerticalAlignment="Top">
            <StackPanel>
                <materialDesign:PackIcon Kind="ShieldLockOutline" Width="55" Height="55" Foreground="#2962FF" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="ĐỔI MẬT KHẨU" FontSize="22" FontWeight="Bold" Foreground="#1A237E" HorizontalAlignment="Center"/>
                <TextBlock Text="Thay đổi mật khẩu định kỳ giúp bảo vệ tài khoản của bạn tốt hơn khỏi các rủi ro." TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="#878A99" Margin="0,10,0,25"/>

                <Grid Margin="0,0,0,20">
                    <PasswordBox helpers:PasswordHelper.Attach="True" helpers:PasswordHelper.Password="{Binding OldPassword}" materialDesign:HintAssist.Hint="Mật khẩu hiện tại" Padding="0,0,30,0" FontSize="15">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToggleOldPwd}" Value="True">
                                        <Setter Property="Visibility" Value="Hidden" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>
                    <TextBox Text="{Binding OldPassword, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Mật khẩu hiện tại" Padding="0,0,30,0" FontSize="15">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
                                <Setter Property="Visibility" Value="Hidden" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToggleOldPwd}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <ToggleButton x:Name="ToggleOldPwd" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,2" Cursor="Hand" ToolTip="Ẩn/Hiện mật khẩu">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border Background="Transparent">
                                    <materialDesign:PackIcon x:Name="Icon" Kind="EyeOutline" Foreground="#878A99" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Icon" Property="Kind" Value="EyeOffOutline"/>
                                        <Setter TargetName="Icon" Property="Foreground" Value="#2962FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <PasswordBox helpers:PasswordHelper.Attach="True" helpers:PasswordHelper.Password="{Binding NewPassword}" materialDesign:HintAssist.Hint="Mật khẩu mới (ít nhất 6 ký tự)" Padding="0,0,30,0" FontSize="15">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToggleNewPwd}" Value="True">
                                        <Setter Property="Visibility" Value="Hidden" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>
                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Mật khẩu mới (ít nhất 6 ký tự)" Padding="0,0,30,0" FontSize="15">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
                                <Setter Property="Visibility" Value="Hidden" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToggleNewPwd}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <ToggleButton x:Name="ToggleNewPwd" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,2" Cursor="Hand" ToolTip="Ẩn/Hiện mật khẩu">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border Background="Transparent">
                                    <materialDesign:PackIcon x:Name="Icon" Kind="EyeOutline" Foreground="#878A99" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Icon" Property="Kind" Value="EyeOffOutline"/>
                                        <Setter TargetName="Icon" Property="Foreground" Value="#2962FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>

                <Grid Margin="0,0,0,35">
                    <PasswordBox helpers:PasswordHelper.Attach="True" helpers:PasswordHelper.Password="{Binding ConfirmPassword}" materialDesign:HintAssist.Hint="Xác nhận mật khẩu mới" Padding="0,0,30,0" FontSize="15">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToggleConfirmPwd}" Value="True">
                                        <Setter Property="Visibility" Value="Hidden" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>
                    <TextBox Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Xác nhận mật khẩu mới" Padding="0,0,30,0" FontSize="15">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
                                <Setter Property="Visibility" Value="Hidden" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToggleConfirmPwd}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <ToggleButton x:Name="ToggleConfirmPwd" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,2" Cursor="Hand" ToolTip="Ẩn/Hiện mật khẩu">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border Background="Transparent">
                                    <materialDesign:PackIcon x:Name="Icon" Kind="EyeOutline" Foreground="#878A99" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Icon" Property="Kind" Value="EyeOffOutline"/>
                                        <Setter TargetName="Icon" Property="Foreground" Value="#2962FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>

                <Button Content="CẬP NHẬT MẬT KHẨU" Command="{Binding ChangePasswordCommand}" Style="{StaticResource PrimaryRoundedButton}" Height="45" FontSize="16" FontWeight="Bold" Cursor="Hand" IsDefault="True"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>