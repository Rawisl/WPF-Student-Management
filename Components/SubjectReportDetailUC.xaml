<UserControl x:Class="WPF_Student_Management.Components.SubjectReportDetailUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_Student_Management.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="850">

    <Border CornerRadius="16"
            Background="White"
            Margin="10"
            MinWidth="800"
            MaxWidth="900"
            MinHeight="500"
            MaxHeight="650">

        <Border.Effect>
            <DropShadowEffect BlurRadius="15"
                              ShadowDepth="3"
                              Opacity="0.2" />
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding SelectedReportRow.ClassName, StringFormat='Danh sách chi tiết môn học - Lớp: {0}'}"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#1A237E"
                           VerticalAlignment="Center" />

                <Button Grid.Column="1"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        Foreground="Gray"
                        Width="35"
                        Height="35"
                        Padding="0"
                        ToolTip="Đóng">
                    <materialDesign:PackIcon Kind="Close"
                                             Width="24"
                                             Height="24" />
                </Button>
            </Grid>

            <Separator Grid.Row="1"
                       Background="#E0E0E0"
                       Margin="0,0,0,20" />

            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding DetailedStudentList}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Style="{StaticResource GlobalDataGridStyle}"
                      CellStyle="{StaticResource GlobalDataGridCellStyle}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      FontSize="15">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="STT"
                                        Binding="{Binding OrderNumber}"
                                        Width="80"
                                        HeaderStyle="{StaticResource GlobalCenterHeaderStyle}"
                                        ElementStyle="{StaticResource GlobalCenterElementStyle}" />

                    <DataGridTextColumn Header="Họ và tên"
                                        Binding="{Binding FullName}"
                                        Width="*"
                                        HeaderStyle="{StaticResource GlobalLeftHeaderStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"
                                   BasedOn="{StaticResource GlobalLeftElementStyle}">
                                <Setter Property="FontWeight"
                                        Value="SemiBold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Điểm TB"
                                        Binding="{Binding AverageScore, StringFormat={}{0:F1}}"
                                        Width="150"
                                        HeaderStyle="{StaticResource GlobalCenterHeaderStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"
                                   BasedOn="{StaticResource GlobalCenterElementStyle}">
                                <Setter Property="FontWeight"
                                        Value="Bold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Kết quả"
                                        Binding="{Binding Result}"
                                        Width="150"
                                        HeaderStyle="{StaticResource GlobalCenterHeaderStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"
                                   BasedOn="{StaticResource GlobalCenterElementStyle}">
                                <Setter Property="FontWeight"
                                        Value="ExtraBold" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Result}"
                                                 Value="Không đạt">
                                        <Setter Property="Foreground"
                                                Value="#D32F2F" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Result}"
                                                 Value="Đạt">
                                        <Setter Property="Foreground"
                                                Value="#1B5E20" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>