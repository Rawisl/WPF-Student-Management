<UserControl x:Class="WPF_Student_Management.Components.GradeEntryUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_Student_Management.Components"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:WPF_Student_Management.Helpers">

    <Grid Background="#F5F6FA"
          Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Nhập bảng điểm môn"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20" />

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  ShadowDepth="1"
                                  Color="#DDDDDD"
                                  Opacity="0.5" />
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.7*" />
                    <ColumnDefinition Width="0.7*" />
                    <ColumnDefinition Width="1.25*" />
                    <ColumnDefinition Width="0.9*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Margin="0,0,20,0">
                    <TextBlock Text="Năm học:"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5"
                               Foreground="Gray" />
                    <ComboBox Height="35"
                              ItemsSource="{Binding DanhSachNamHocReport}" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Margin="0,0,20,0">
                    <TextBlock Text="Học kỳ:"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5"
                               Foreground="Gray" />
                    <ComboBox Height="35"
                              ItemsSource="{Binding DanhSachHocKyReport}" />
                </StackPanel>

                <StackPanel Grid.Column="2"
                            Margin="0,0,20,0">
                    <TextBlock Text="Môn học:"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5"
                               Foreground="Gray" />
                    <ComboBox Height="35"
                              ItemsSource="{Binding DanhSachMonReport}" />
                </StackPanel>

                <StackPanel Grid.Column="3"
                            Margin="0,0,20,0">
                    <TextBlock Text="Lớp:"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5"
                               Foreground="Gray" />
                    <ComboBox Height="35"
                              ItemsSource="{Binding DanhSachHocKyReport}" />
                </StackPanel>

                <Button Content="Lấy danh sách"
                        Grid.Column="4"
                        Margin="5,0,0,0"
                        Command="{Binding GenerateReportCommand}"
                        Style="{StaticResource PrimaryRoundedButton}"
                        VerticalAlignment="Bottom">
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="8"
                Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  ShadowDepth="1"
                                  Color="#DDDDDD"
                                  Opacity="0.5" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Margin="0,0,0,15">
                    <TextBlock Text="Nhập điểm môn Toán - Lớp 10A1 - Học kỳ 1"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center" />
                    <Button Content="Lưu bảng điểm"
                            Background="#00B894"
                            Foreground="White"
                            HorizontalAlignment="Right"
                            Height="40"
                            Width="150"
                            Cursor="Hand"
                            Command="{Binding SaveGradeDataCommand}"
                            Style="{StaticResource PrimaryRoundedButton}">
                    </Button>
                </Grid>

                <DataGrid Grid.Row="1"
                          AutoGenerateColumns="False"
                          ItemsSource="{Binding DanhSachHocSinh}"
                          CanUserAddRows="False"
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeight="50"
                          ColumnHeaderHeight="45"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT"
                                            Binding="{Binding OrdinalNumber}"
                                            IsReadOnly="True"
                                            Width="80" />

                        <DataGridTextColumn Header="Họ và tên"
                                            Binding="{Binding FullName}"
                                            IsReadOnly="True"
                                            Width="*" />

                        <DataGridTextColumn Header="Điểm 15'"
                                            Binding="{Binding Diem15Phut}"
                                            IsReadOnly="False"
                                            Width="120">
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="helpers:TextBoxHelper.IsDecimalOnly"
                                            Value="True" />
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Điểm 1 tiết"
                                            Binding="{Binding Diem1Tiet}"
                                            IsReadOnly="False"
                                            Width="120">
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="helpers:TextBoxHelper.IsDecimalOnly"
                                            Value="True" />
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Điểm TB"
                                            Binding="{Binding DiemTB,StringFormat={}{0:F1}}"
                                            IsReadOnly="True"
                                            Width="120"
                                            Foreground="Gray" />
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</UserControl>
