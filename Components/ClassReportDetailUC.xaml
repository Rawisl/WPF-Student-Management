<UserControl x:Class="WPF_Student_Management.Components.ClassReportDetailUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <DataGrid ItemsSource="{Binding DetailList}" 
              IsReadOnly="True" 
              Style="{StaticResource GlobalDataGridStyle}" 
              CellStyle="{StaticResource GlobalDataGridCellStyle}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="80" HeaderStyle="{StaticResource GlobalCenterHeaderStyle}" ElementStyle="{StaticResource GlobalCenterElementStyle}" />

            <DataGridTextColumn Header="Mã HS" Binding="{Binding StudentId}" Width="150" HeaderStyle="{StaticResource GlobalCenterHeaderStyle}" ElementStyle="{StaticResource GlobalCenterElementStyle}" />

            <DataGridTextColumn Header="Họ và Tên" Binding="{Binding FullName}" Width="*" HeaderStyle="{StaticResource GlobalLeftHeaderStyle}">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource GlobalLeftElementStyle}">
                        <Setter Property="FontWeight" Value="SemiBold" />
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTemplateColumn Header="Xếp loại Học kỳ" Width="200" HeaderStyle="{StaticResource GlobalLeftHeaderStyle}">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Status}" FontWeight="Bold" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#1B5E20" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Không đạt">
                                            <Setter Property="Foreground" Value="#D32F2F" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>