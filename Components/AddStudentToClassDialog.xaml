<UserControl x:Class="WPF_Student_Management.Components.AddStudentToClassDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_Student_Management.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <Border Background="White"
            CornerRadius="16"
            MinWidth="500"
            MaxWidth="500"
            MinHeight="550"
            MaxHeight="700"
            Padding="25">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Danh sách học sinh chưa được xếp lớp"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0,0,0,20"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"/>

            <Button Grid.Row="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Style="{StaticResource MaterialDesignIconForegroundButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Foreground="Gray"
                    Width="30"
                    Height="30"
                    Padding="0"
                    Margin="0,-5,-5,0">
                <materialDesign:PackIcon Kind="Close"
                                         Width="22"
                                         Height="22" />
            </Button>

            <!-- Thêm SelectionMode="Multiple" -->
            <ListBox Grid.Row="1"
                     ItemsSource="{Binding AvailableStudents}"
                     SelectionMode="Multiple"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem"
                           BasedOn="{StaticResource MaterialDesignListBoxItem}">
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch" />
                        <!-- Ràng buộc trạng thái dòng với biến IsSelected -->
                        <Setter Property="IsSelected"
                                Value="{Binding IsSelected, Mode=TwoWay}" />
                        <!-- Tạo khoảng cách giữa các dòng -->
                        <Setter Property="Margin"
                                Value="0,0,0,10" />
                        <Setter Property="Padding"
                                Value="0" />
                        <Setter Property="BorderThickness"
                                Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Làm tròn góc, thêm nền xám nhạt cho từng khối học sinh -->
                        <Border Background="#F8F9FA"
                                BorderBrush="#E9ECEF"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="15,10"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- IsHitTestVisible="False" giúp click không bị kẹt ở checkbox -->
                                <CheckBox Grid.Column="0"
                                          IsChecked="{Binding IsSelected}"
                                          IsHitTestVisible="False"
                                          VerticalAlignment="Center" />

                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Margin="10,0,0,0">
                                    <TextBlock Text="{Binding FullName}"
                                               FontWeight="SemiBold"
                                               FontSize="16"
                                               Foreground="#2C3E50" />
                                    <!-- Đã sửa lại StudentId cho khớp với ViewModel -->
                                    <TextBlock Text="{Binding StudentId}"
                                               Foreground="Gray"
                                               FontSize="13"
                                               Margin="0,2,0,0" />
                                </StackPanel>

                                <StackPanel Grid.Column="2"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Gender}"
                                               HorizontalAlignment="Right"
                                               Foreground="#34495E" />
                                    <TextBlock Text="{Binding DateOfBirth}"
                                               Foreground="Gray"
                                               FontSize="13"
                                               Margin="0,2,0,0"
                                               HorizontalAlignment="Right" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2"
                    Content="Lưu lựa chọn"
                    Margin="0,20,0,0"
                    Height="45"
                    FontSize="22"
                    FontWeight="SemiBold"
                    Command="{Binding SaveSelectionCommand}"
                    Style="{StaticResource PrimaryRoundedButton}" />
        </Grid>
    </Border>
</UserControl>